{
  "exercise1": {
    "prompt": "Customize your hello message",
    "cases": [
      { "name": "prints something", "nonempty": true },
      {
        "name": "uses print() rather than just writing text",
        "source_matches": "print\\s*\\(",
        "describe": "a call to print()"
      }
    ],
    "hint": "Put your message inside print(), with the text in quotes: print(\"your message here\")."
  },
  "exercise2": {
    "prompt": "Print three separate lines",
    "cases": [
      { "name": "prints at least three lines", "min_lines": 3 },
      {
        "name": "uses a separate print() for each line",
        "source_matches": "(?:print\\s*\\([\\s\\S]*?){3}",
        "describe": "three print() statements"
      }
    ],
    "hint": "Each line needs its own print() on its own line. Three lines of output means three print() statements."
  }
}
