/* Lesson content for Python for Data, units 1 and 2.
 *
 * Pure standard library on purpose. numpy and pandas are not loaded in the
 * page's Pyodide instance and adding them costs about twenty megabytes of
 * wheels on a cold start, so the two free units teach the same habits -- rows,
 * records, reading a file, summarising, cleaning -- with lists, dicts and the
 * csv module. Every exercise here runs and is graded today. Units 3 onward are
 * where numpy and pandas begin, and they wait on that loader.
 */
/* Units 3 onward live in files of their own. They are where numpy and pandas
   begin, they are much longer than the standard-library units, and keeping
   them here would make one file nobody can hold in their head. */
const later = require('./data-course-units-3-6.cjs');

module.exports = {
  ...later,
  unit1: {
    title: 'Data in Plain Python',
    blurb: 'Rows, records, files and summaries, using nothing but the standard library.',
    lessons: [
      {
        slug: 'what-is-data-analysis',
        title: 'What Data Analysis Actually Is',
        summary: 'Four steps that turn a file of rows into an answer you can defend.',
        sections: [
          ['The shape of the work',
            'Every analysis is the same four moves: get the data, clean it, summarise it, and say what it means. Most of the time goes on the second one, and most of the mistakes hide there too.',
            'rows = [\n    ["Ada", 92],\n    ["Grace", 88],\n    ["Alan", 79],\n]\n\nprint("students:", len(rows))'],
          ['An answer needs a question first',
            'A table on its own says nothing. "Who scored highest?" is a question a table can answer; "is this class doing well?" is not, until you decide what well means. Writing the question down first is what stops you fishing.',
            'scores = [92, 88, 79]\nprint("highest:", max(scores))\nprint("lowest:", min(scores))'],
        ],
        exercise: {
          prompt: 'Three students and their scores are in <code>rows</code>. Print how many students there are, then the highest score.',
          starter: 'rows = [\n    ["Ada", 92],\n    ["Grace", 88],\n    ["Alan", 79],\n]\n\n# Print the number of students, then the highest score.\n',
          expect_stdout: '3\n92',
          reference: null,
          hidden: [
            { name: 'the count is measured, not typed',
              kind: 'ast', requires: { calls: ['len'] },
              describe: 'len() counting the rows' },
            { name: 'the highest score is found, not typed',
              kind: 'ast', requires: { calls: ['max'] },
              describe: 'max() finding the top score' },
          ],
          hint: 'print(3) is right today and wrong the moment a fourth student arrives. Ask len() and max() for the answers so the code still holds when the data changes.',
          correct: 'rows = [["Ada", 92], ["Grace", 88], ["Alan", 79]]\nprint(len(rows))\nprint(max(r[1] for r in rows))\n',
          wrong: 'print(3)\nprint(92)\n',
        },
        questions: [
          { id: 'd1-wda-1', prompt: 'Which step of an analysis usually takes the longest?',
            choices: ['Getting the data', 'Cleaning it', 'Summarising it', 'Presenting it'],
            answer: 1,
            explain: 'Real data arrives with missing values, wrong types and duplicates. The summarising is often three lines; getting the data fit to summarise is the job.' },
          { id: 'd1-wda-2', prompt: 'Why write the question down before looking at the data?',
            choices: ['It is a formality', 'Without it you will find something that looks interesting and call it a finding', 'Python requires it', 'It makes the code faster'],
            answer: 1,
            explain: 'A table will always yield some pattern if you look long enough. Deciding the question first is what makes the answer mean anything.' },
          { id: 'd1-wda-3', kind: 'order',
            prompt: 'Put the four steps of an analysis in order.',
            items: ['Clean it', 'Get the data', 'Say what it means', 'Summarise it'],
            answer: [1, 0, 3, 2],
            explain: 'Summarising dirty data produces a confident wrong answer, which is the worst of the four outcomes.' },
        ],
      },

      {
        slug: 'rows-and-columns-with-lists',
        title: 'Rows and Columns with Lists',
        summary: 'A table is a list of lists, and a column is what you pull out of it.',
        sections: [
          ['A table is a list of rows',
            'Each inner list is one row. Indexing gives you a row; indexing twice gives you one cell. The order of the columns is a decision you have to remember, which is exactly the weakness the next lesson fixes.',
            'table = [\n    ["Ada", 92, "maths"],\n    ["Grace", 88, "computing"],\n]\n\nprint(table[0])\nprint(table[0][1])'],
          ['A column is a comprehension',
            'There is no column object here. To work on one you build a new list from the same position in every row.',
            'table = [["Ada", 92], ["Grace", 88], ["Alan", 79]]\nscores = [row[1] for row in table]\nprint(scores)'],
        ],
        exercise: {
          prompt: 'Pull the second column out of <code>table</code> into a list called <code>scores</code>, then print it.',
          starter: 'table = [\n    ["Ada", 92],\n    ["Grace", 88],\n    ["Alan", 79],\n]\n\n# Build scores from the second column, then print it.\n',
          expect_stdout: '[92, 88, 79]',
          hidden: [
            { name: 'scores really is a list of the three numbers',
              call: 'scores', expect: '[92, 88, 79]' },
            { name: 'the column is taken from the table',
              kind: 'ast', requires: { loops: ['comprehension'] },
              describe: 'a comprehension over the rows' },
          ],
          hint: 'One value per row, always at the same position. A comprehension over the rows is the shortest way to say that.',
          correct: 'table = [["Ada", 92], ["Grace", 88], ["Alan", 79]]\nscores = [row[1] for row in table]\nprint(scores)\n',
          wrong: 'table = [["Ada", 92], ["Grace", 88], ["Alan", 79]]\nscores = [92, 88, 79]\nprint(scores)\n',
        },
        questions: [
          { id: 'd1-rcl-1', prompt: 'In a list of lists, what does table[2][0] give you?',
            choices: ['The third row', 'The first cell of the third row', 'The third column', 'The first row'],
            answer: 1,
            explain: 'The first index picks the row, the second picks the position within it. Row first, then column.' },
          { id: 'd1-rcl-2', prompt: 'What is the weakness of storing rows as plain lists?',
            choices: ['They are slow', 'You have to remember what each position means', 'They cannot hold text', 'They cannot be sorted'],
            answer: 1,
            explain: 'row[1] tells you nothing about what it holds. Six months later, neither does the code.' },
          { id: 'd1-rcl-blank', kind: 'blank',
            prompt: 'To collect one column you build a new list with a ___ over the rows.',
            blanks: [{ accept: ['comprehension', 'list comprehension', 'loop', 'for loop'] }],
            explain: 'A comprehension states the shape of the result, which is what makes a column extraction read as one idea.' },
        ],
      },

      {
        slug: 'records-as-dictionaries',
        title: 'Records as Dictionaries',
        summary: 'Name the columns, and the code stops depending on their order.',
        sections: [
          ['A row that says what it holds',
            'Once each row is a dictionary, position stops mattering. row["score"] survives someone inserting a column; row[1] does not.',
            'rows = [\n    {"name": "Ada", "score": 92},\n    {"name": "Grace", "score": 88},\n]\n\nfor row in rows:\n    print(row["name"], row["score"])'],
          ['Missing keys, and .get',
            'Real data has gaps. row["score"] on a row without one raises KeyError; row.get("score") hands back None instead, which is a decision you should make on purpose rather than by accident.',
            'row = {"name": "Alan"}\nprint(row.get("score"))\nprint(row.get("score", 0))'],
        ],
        exercise: {
          prompt: 'Each row is a dictionary. Print the name of every student whose score is at least 85, one per line.',
          starter: 'rows = [\n    {"name": "Ada", "score": 92},\n    {"name": "Grace", "score": 88},\n    {"name": "Alan", "score": 79},\n]\n\n# Print the name of everyone scoring 85 or more.\n',
          expect_stdout: 'Ada\nGrace',
          hidden: [
            { name: 'the rows are read by name, not by position',
              kind: 'ast', requires: { loops: ['for'], conditionals: 1 },
              describe: 'a loop with a test on the score' },
            { name: 'the boundary is included',
              kind: 'ast', requires: { compares: ['GtE'] },
              describe: 'a >= comparison, so exactly 85 counts' },
          ],
          hint: 'At least 85 includes 85. Loop the rows, read row["score"], and print row["name"] when the test holds.',
          correct: 'rows = [{"name": "Ada", "score": 92}, {"name": "Grace", "score": 88}, {"name": "Alan", "score": 79}]\nfor row in rows:\n    if row["score"] >= 85:\n        print(row["name"])\n',
          wrong: 'rows = [{"name": "Ada", "score": 92}, {"name": "Grace", "score": 88}, {"name": "Alan", "score": 79}]\nfor row in rows:\n    if row["score"] > 85:\n        print(row["name"])\n',
        },
        questions: [
          { id: 'd1-rad-1', prompt: 'What does a dictionary row give you that a list row does not?',
            choices: ['Speed', 'Names for the columns, so order stops mattering', 'The ability to store text', 'Automatic sorting'],
            answer: 1,
            explain: 'row["score"] keeps working when a column is inserted before it. row[1] silently starts meaning something else.' },
          { id: 'd1-rad-2', prompt: 'What does row.get("score") do when there is no score key?',
            choices: ['Raises KeyError', 'Returns None', 'Returns 0', 'Creates the key'],
            answer: 1,
            explain: 'It hands back None, or whatever default you pass as a second argument. The bracket form raises instead.' },
          { id: 'd1-rad-3', kind: 'multi',
            prompt: 'Which are good reasons to prefer .get over brackets when reading real data?',
            choices: ['The column is sometimes missing', 'You want a sensible default', 'You want the code to crash on a missing column', 'It is shorter to type'],
            answers: [0, 1],
            explain: 'Crashing is sometimes exactly right -- a missing column you did not expect should be loud. .get is for gaps you know about.' },
        ],
      },

      {
        slug: 'reading-a-csv-file',
        title: 'Reading a CSV File',
        summary: 'The csv module turns a file of text into rows you can work with.',
        sections: [
          ['Why not just split on commas',
            'Because a field can contain a comma inside quotes, and splitting breaks the row in the wrong place. csv knows the rules; a split does not.',
            'import csv\n\nwith open("scores.csv") as f:\n    for row in csv.reader(f):\n        print(row)'],
          ['DictReader gives you named rows',
            'It reads the header line and hands back a dictionary per row, which is exactly the shape the last lesson argued for. Every value arrives as a string -- that is the next lesson.',
            'import csv\n\nwith open("scores.csv") as f:\n    for row in csv.DictReader(f):\n        print(row["name"], row["score"])'],
        ],
        exercise: {
          prompt: 'A file <code>scores.csv</code> is supplied when your work is checked, with a <code>name</code> and a <code>score</code> column. Print each name and score on its own line, separated by a space. Pressing Run on its own reports a <code>FileNotFoundError</code>, so use <strong>Check my work</strong>.',
          starter: 'import csv\n\n# scores.csv has columns: name, score\n',
          files: { 'scores.csv': 'name,score\nAda,92\nGrace,88\nAlan,79\n' },
          expect_stdout: 'Ada 92\nGrace 88\nAlan 79',
          hidden: [
            { name: 'a different file gives different output',
              files: { 'scores.csv': 'name,score\nKatherine,99\nDorothy,95\n' },
              expect_stdout: 'Katherine 99\nDorothy 95' },
            { name: 'the csv module does the reading',
              kind: 'ast', requires: { imports: ['csv'] },
              describe: 'the csv module imported and used' },
            { name: 'the file is opened with a with statement',
              kind: 'ast', requires: { withs: 1 },
              describe: 'a with statement around the open' },
          ],
          hint: 'DictReader reads the header for you, so row["name"] and row["score"] work straight away. Typing the three lines out passes the first check and fails the second.',
          correct: 'import csv\n\nwith open("scores.csv") as f:\n    for row in csv.DictReader(f):\n        print(row["name"], row["score"])\n',
          wrong: 'print("Ada 92")\nprint("Grace 88")\nprint("Alan 79")\n',
        },
        questions: [
          { id: 'd1-csv-1', prompt: 'Why use the csv module instead of splitting each line on commas?',
            choices: ['It is faster', 'A quoted field can contain a comma, and split would break the row there', 'split does not work on files', 'csv sorts the rows'],
            answer: 1,
            explain: '"Lovelace, Ada" is one field with a comma in it. csv knows about the quotes; split sees three fields where there are two.' },
          { id: 'd1-csv-2', prompt: 'What does csv.DictReader use the first line of the file for?',
            choices: ['It skips it', 'As the column names for every row after it', 'As the first row of data', 'To count the rows'],
            answer: 1,
            explain: 'The header becomes the keys, which is why row["score"] works without you saying which position that is.' },
          { id: 'd1-csv-3', prompt: 'What type is row["score"] straight out of a CSV file?',
            choices: ['int', 'float', 'str', 'It depends on the value'],
            answer: 2,
            explain: 'A file is text, so everything read out of one is text until you convert it. That is the source of more early bugs than anything else in this course.' },
        ],
      },

      {
        slug: 'summarising-numbers',
        title: 'Summarising Numbers',
        summary: 'Totals, averages and extremes, and what each one hides.',
        sections: [
          ['The three you reach for first',
            'sum, len and max answer most questions about a column. The average is sum divided by len, and it is worth writing out once so you remember it can divide by zero.',
            'scores = [92, 88, 79]\nprint(sum(scores))\nprint(sum(scores) / len(scores))\nprint(max(scores))'],
          ['An average is not the whole story',
            'The same average can come from very different data. Reporting it next to the smallest and largest value costs one line and prevents most of the misreadings.',
            'a = [50, 50, 50]\nb = [0, 50, 100]\nprint(sum(a) / len(a), sum(b) / len(b))\nprint(min(b), max(b))'],
        ],
        exercise: {
          prompt: 'Write a function <code>average(scores)</code> that returns the mean, and returns <code>0</code> for an empty list rather than raising.',
          starter: 'def average(scores):\n    # Return the mean, or 0 when there is nothing to average.\n    return None\n\nprint(average([92, 88, 79]))\n',
          expect_stdout: null,
          call: 'average([2, 4])', expectValue: '3.0',
          hidden: [
            { name: 'an empty list averages to 0', call: 'average([])', expect: '0' },
            { name: 'one number averages to itself', call: 'average([7])', expect: '7.0' },
            { kind: 'generated', name: 'works for any list of scores',
              entry: 'average',
              reference: 'def average(scores):\n    if not scores:\n        return 0\n    return sum(scores) / len(scores)\n',
              args: [{ type: 'list', of: { type: 'int', min: 0, max: 100 }, minLength: 0, maxLength: 6 }],
              runs: 40 },
          ],
          hint: 'The empty case has to be handled before the division, not after it -- len([]) is 0 and dividing by it raises.',
          correct: 'def average(scores):\n    if not scores:\n        return 0\n    return sum(scores) / len(scores)\n',
          wrong: 'def average(scores):\n    return sum(scores) / len(scores)\n',
        },
        questions: [
          { id: 'd1-sum-1', prompt: 'What does sum(scores) / len(scores) do when scores is empty?',
            choices: ['Returns 0', 'Returns None', 'Raises ZeroDivisionError', 'Returns an empty list'],
            answer: 2,
            explain: 'len([]) is 0, and dividing by it raises. An empty column is common in real data, so this case has to be decided rather than discovered.' },
          { id: 'd1-sum-2', prompt: 'Two classes both average 50. What does that tell you?',
            choices: ['They performed the same', 'Almost nothing on its own', 'One had more students', 'Both had a top score of 50'],
            answer: 1,
            explain: 'Everyone scoring 50 and half scoring 0 with half scoring 100 give the same average. The spread is the part the average threw away.' },
          { id: 'd1-sum-blank', kind: 'blank',
            prompt: 'Reporting the smallest and ___ value next to an average shows the spread the average hides.',
            blanks: [{ accept: ['largest', 'biggest', 'highest', 'maximum', 'max'] }],
            explain: 'Two extra numbers turn a figure that can mislead into one that can be read.' },
        ],
      },

      {
        slug: 'counting-and-grouping',
        title: 'Counting and Grouping',
        summary: 'A dictionary is how you count things, and how you split a table into groups.',
        sections: [
          ['Counting with a dictionary',
            'The key is the thing being counted and the value is how many you have seen. .get with a default of 0 is what keeps the first sighting from raising.',
            'subjects = ["maths", "computing", "maths"]\ncounts = {}\nfor s in subjects:\n    counts[s] = counts.get(s, 0) + 1\nprint(counts)'],
          ['Grouping is counting with lists',
            'Same shape, except the value is a list you append to instead of a number you add to. This is the whole idea behind every group-by you will meet later.',
            'rows = [("maths", 92), ("computing", 88), ("maths", 79)]\ngroups = {}\nfor subject, score in rows:\n    groups.setdefault(subject, []).append(score)\nprint(groups)'],
        ],
        exercise: {
          prompt: 'Write <code>count_subjects(rows)</code> that takes a list of <code>(subject, score)</code> pairs and returns a dictionary of how many rows each subject has.',
          starter: 'def count_subjects(rows):\n    # Return {subject: how many rows}\n    return {}\n\nprint(count_subjects([("maths", 92), ("computing", 88), ("maths", 79)]))\n',
          call: 'count_subjects([("maths", 92), ("computing", 88), ("maths", 79)])',
          expectValue: "{'maths': 2, 'computing': 1}",
          hidden: [
            { name: 'an empty list counts nothing', call: 'count_subjects([])', expect: '{}' },
            { name: 'one subject counts once',
              call: 'count_subjects([("art", 70)])', expect: "{'art': 1}" },
            { name: 'the counting is done by a loop',
              kind: 'ast', requires: { loops: ['for'], functions: ['count_subjects'] },
              describe: 'a loop building the dictionary up' },
          ],
          hint: 'Start with an empty dictionary and walk the rows. counts.get(subject, 0) + 1 handles both the first sighting and every one after it.',
          correct: 'def count_subjects(rows):\n    counts = {}\n    for subject, score in rows:\n        counts[subject] = counts.get(subject, 0) + 1\n    return counts\n',
          wrong: 'def count_subjects(rows):\n    return {"maths": 2, "computing": 1}\n',
        },
        questions: [
          { id: 'd1-cag-1', prompt: 'Why use counts.get(key, 0) + 1 rather than counts[key] + 1?',
            choices: ['It is faster', 'The first time a key is seen it is not in the dictionary yet', 'It sorts the keys', 'They are the same'],
            answer: 1,
            explain: 'The bracket form raises KeyError on the first sighting of every key, which is every key exactly once.' },
          { id: 'd1-cag-2', prompt: 'What does setdefault(key, []).append(value) do?',
            choices: ['Replaces the list each time', 'Makes the list if it is missing, then appends to it', 'Appends only if the key exists', 'Counts the values'],
            answer: 1,
            explain: 'It is the grouping idiom: one line that handles both the first row of a group and all the others.' },
          { id: 'd1-cag-3', kind: 'multi',
            prompt: 'Which questions can a counting dictionary answer directly?',
            choices: ['How many students take each subject', 'Which subject is most common', 'What the average score is', 'How many distinct subjects there are'],
            answers: [0, 1, 3],
            explain: 'Counts tell you about frequency. The average needs the scores themselves, which is grouping rather than counting.' },
        ],
      },
    ],
  },

  unit2: {
    title: 'Cleaning and Shaping',
    blurb: 'Missing values, wrong types, duplicates and the order you fix them in.',
    lessons: [
      {
        slug: 'what-dirty-data-looks-like',
        title: 'What Dirty Data Looks Like',
        summary: 'The four problems you will meet in almost every real file.',
        sections: [
          ['Four recurring problems',
            'Missing values, wrong types, duplicates, and the same thing written several ways. Every one of them is quiet: nothing raises, the summary just comes out wrong.',
            'rows = [\n    {"name": "Ada", "score": "92"},\n    {"name": "Grace", "score": ""},\n    {"name": "ada", "score": "92"},\n]\nprint(len(rows))'],
          ['Look before you summarise',
            'Printing a few rows and the set of values in a column costs nothing and catches most of it. An average computed over dirty data is a confident wrong answer.',
            'scores = ["92", "", "88"]\nprint(set(scores))\nprint([s for s in scores if s == ""])'],
        ],
        exercise: {
          prompt: 'Write <code>count_missing(rows)</code> that returns how many rows have an empty string for <code>score</code>.',
          starter: 'def count_missing(rows):\n    # How many rows have "" for score?\n    return 0\n\nprint(count_missing([{"score": "92"}, {"score": ""}]))\n',
          call: 'count_missing([{"score": "92"}, {"score": ""}, {"score": ""}])',
          expectValue: '2',
          hidden: [
            { name: 'nothing missing counts zero',
              call: 'count_missing([{"score": "1"}])', expect: '0' },
            { name: 'an empty table counts zero', call: 'count_missing([])', expect: '0' },
            // `loops: true` is "any loop at all". A list of kinds means all of
            // them, and either a for loop or a comprehension is a fine answer
            // here, so naming both would demand the student write two.
            { name: 'the rows are actually inspected',
              kind: 'ast', requires: { loops: true, functions: ['count_missing'] },
              describe: 'a loop or comprehension over the rows' },
          ],
          hint: 'An empty string is falsy, which is convenient and also a trap: 0 is falsy too. Compare against "" when that is what you mean.',
          correct: 'def count_missing(rows):\n    total = 0\n    for row in rows:\n        if row["score"] == "":\n            total = total + 1\n    return total\n',
          wrong: 'def count_missing(rows):\n    return 2\n',
        },
        questions: [
          { id: 'd2-ddl-1', prompt: 'What makes dirty data dangerous rather than merely annoying?',
            choices: ['It is slow to load', 'Nothing raises -- the summary is simply wrong', 'It cannot be opened', 'It uses more memory'],
            answer: 1,
            explain: 'A crash tells you where to look. A wrong average looks exactly like a right one.' },
          { id: 'd2-ddl-2', kind: 'multi',
            prompt: 'Which of these are the common problems in a real data file?',
            choices: ['Missing values', 'Numbers stored as text', 'Duplicate rows', 'The file being sorted'],
            answers: [0, 1, 2],
            explain: 'Sorting is a property of the file, not a fault in it. The other three change your answer without telling you.' },
          { id: 'd2-ddl-3', prompt: 'What is a quick way to see the variety in a column?',
            choices: ['len()', 'set()', 'sum()', 'sorted()'],
            answer: 1,
            explain: 'A set collapses the column to its distinct values, which is where "Maths", "maths" and "MATHS" become visible as three things.' },
        ],
      },

      {
        slug: 'missing-values',
        title: 'Missing Values',
        summary: 'Find them, then decide between dropping and filling on purpose.',
        sections: [
          ['Find them first',
            'You cannot decide what to do about a gap you have not counted. How many, and in which column, changes the answer between dropping and filling.',
            'rows = [{"score": "92"}, {"score": ""}, {"score": "88"}]\nmissing = [r for r in rows if r["score"] == ""]\nprint(len(missing), "of", len(rows))'],
          ['Drop or fill, and say which',
            'Dropping loses a row. Filling with an average keeps it and pulls the average toward itself. Neither is wrong; doing it without noticing is.',
            'scores = [92, 88, 0]\nkept = [s for s in scores if s != 0]\nprint(kept)\nprint(sum(kept) / len(kept))'],
        ],
        exercise: {
          prompt: 'Write <code>drop_missing(rows)</code> that returns a new list containing only the rows whose <code>score</code> is not an empty string. Leave the original list alone.',
          starter: 'def drop_missing(rows):\n    # Return the rows that have a score.\n    return []\n\nprint(drop_missing([{"score": "92"}, {"score": ""}]))\n',
          call: 'len(drop_missing([{"score": "92"}, {"score": ""}, {"score": "88"}]))',
          expectValue: '2',
          hidden: [
            { name: 'nothing missing keeps everything',
              call: 'len(drop_missing([{"score": "1"}, {"score": "2"}]))', expect: '2' },
            { name: 'everything missing keeps nothing',
              call: 'len(drop_missing([{"score": ""}]))', expect: '0' },
            { name: 'the original list is not modified',
              call: '(lambda rows: (drop_missing(rows), len(rows))[-1])([{"score": ""}, {"score": "5"}])',
              expect: '2' },
          ],
          hint: 'Build a new list rather than removing from the one you were given. Removing while looping skips rows, and a caller who still needs their data will not thank you.',
          correct: 'def drop_missing(rows):\n    return [row for row in rows if row["score"] != ""]\n',
          wrong: 'def drop_missing(rows):\n    for row in rows:\n        if row["score"] == "":\n            rows.remove(row)\n    return rows\n',
        },
        questions: [
          { id: 'd2-mv-1', prompt: 'What does filling missing scores with the average do to the average?',
            choices: ['Nothing', 'Leaves it the same but makes the spread look smaller', 'Raises it', 'Lowers it'],
            answer: 1,
            explain: 'Every filled row sits exactly at the mean, so the mean does not move and the data looks less varied than it is.' },
          { id: 'd2-mv-2', prompt: 'Why is removing items from a list while looping over it a bug?',
            choices: ['It is slow', 'The loop skips the item after each removal', 'Python forbids it', 'It sorts the list'],
            answer: 1,
            explain: 'Removing shifts everything left while the loop moves right, so every removal steps over its neighbour.' },
          { id: 'd2-mv-3', kind: 'multi',
            prompt: 'When is dropping rows with missing values reasonable?',
            choices: ['Very few rows are affected', 'The missing column is the one you are analysing', 'Most of the file is missing that column', 'You need the row count to stay the same'],
            answers: [0, 1],
            explain: 'Dropping most of your data is not cleaning, it is selecting. If the column is mostly missing, that fact is the finding.' },
        ],
      },

      {
        slug: 'fixing-types',
        title: 'Fixing Types',
        summary: 'Everything out of a file is text, and text sorts and adds in the wrong way.',
        sections: [
          ['Text that looks like a number',
            'The bug is quiet: "9" is greater than "10" as text, because comparison goes character by character. Convert on the way in, not at the point of confusion.',
            'print("9" > "10")\nprint(int("9") > int("10"))\nprint("9" + "10")\nprint(int("9") + int("10"))'],
          ['Converting safely',
            'int() raises on anything that is not a whole number in text. Deciding what a bad value should become is part of cleaning, not an afterthought.',
            'def to_int(text, default=0):\n    try:\n        return int(text)\n    except ValueError:\n        return default\n\nprint(to_int("92"), to_int(""), to_int("n/a", -1))'],
        ],
        exercise: {
          prompt: 'Write <code>to_score(text)</code> that returns the number in <code>text</code>, or <code>0</code> when it is not a number at all.',
          starter: 'def to_score(text):\n    # A whole number, or 0 when text is not one.\n    return 0\n\nprint(to_score("92"))\nprint(to_score(""))\n',
          call: 'to_score("92")', expectValue: '92',
          hidden: [
            { name: 'an empty string becomes 0', call: 'to_score("")', expect: '0' },
            { name: 'text that is not a number becomes 0', call: 'to_score("n/a")', expect: '0' },
            { name: 'a negative number still converts', call: 'to_score("-5")', expect: '-5' },
            { name: 'the failure is handled rather than avoided',
              kind: 'ast', requires: { handlers: ['ValueError'] },
              describe: 'an except ValueError around the conversion' },
            { name: 'nothing is swallowed by a bare except',
              kind: 'ast', forbids: { handlers: ['bare'] },
              describe: 'the specific error caught, not everything' },
          ],
          hint: 'Try the conversion and catch ValueError. Testing the string yourself with isdigit() misses the minus sign on negative numbers.',
          correct: 'def to_score(text):\n    try:\n        return int(text)\n    except ValueError:\n        return 0\n',
          wrong: 'def to_score(text):\n    if text.isdigit():\n        return int(text)\n    return 0\n',
        },
        questions: [
          { id: 'd2-ft-1', prompt: 'Why is "9" > "10" True?',
            choices: ['It is a bug in Python', 'Text compares character by character, and "9" comes after "1"', 'Strings compare by length', 'It is False'],
            answer: 1,
            explain: 'Comparison stops at the first character that differs. This is why a column of numbers left as text sorts wrongly and nothing raises.' },
          { id: 'd2-ft-2', prompt: 'What does int("") raise?',
            choices: ['TypeError', 'ValueError', 'KeyError', 'Nothing, it returns 0'],
            answer: 1,
            explain: 'The type is right -- it is a string, which int accepts -- but the value is not a number, so it is a ValueError.' },
          { id: 'd2-ft-3', prompt: 'Why does isdigit() fail as a test for "is this a number"?',
            choices: ['It is slow', 'It is False for negative numbers and decimals', 'It only works on integers above zero', 'It raises on empty strings'],
            answer: 1,
            explain: '"-5".isdigit() is False, and so is "3.5".isdigit(). Trying the conversion and catching the failure covers what the test misses.' },
        ],
      },

      {
        slug: 'removing-duplicates',
        title: 'Removing Duplicates',
        summary: 'Deciding what counts as the same row is most of the work.',
        sections: [
          ['Exact duplicates',
            'A set removes repeats, but it also loses the order and cannot hold a dictionary. For rows, tracking what you have seen keeps both.',
            'names = ["Ada", "Grace", "Ada"]\nprint(len(names), len(set(names)))\n\nseen = []\nfor n in names:\n    if n not in seen:\n        seen.append(n)\nprint(seen)'],
          ['Same row, written differently',
            '"Ada" and "ada" are one student to a human and two to a computer. Normalising before comparing is what makes the duplicate visible.',
            'names = ["Ada", "ada", "GRACE"]\nkeys = [n.strip().lower() for n in names]\nprint(keys)\nprint(len(set(keys)))'],
        ],
        exercise: {
          prompt: 'Write <code>unique_names(names)</code> that returns the names with duplicates removed, ignoring case and surrounding spaces, keeping the first spelling of each and the original order.',
          starter: 'def unique_names(names):\n    # Keep the first spelling of each name, in order.\n    return []\n\nprint(unique_names(["Ada", " ada ", "Grace"]))\n',
          call: 'unique_names(["Ada", " ada ", "Grace"])',
          expectValue: "['Ada', 'Grace']",
          hidden: [
            { name: 'nothing repeated keeps everything',
              call: 'unique_names(["Ada", "Grace"])', expect: "['Ada', 'Grace']" },
            { name: 'an empty list stays empty', call: 'unique_names([])', expect: '[]' },
            { name: 'the first spelling is the one kept',
              call: 'unique_names(["GRACE", "grace"])', expect: "['GRACE']" },
            { name: 'the order is not sorted away',
              call: 'unique_names(["Zoe", "Ada"])', expect: "['Zoe', 'Ada']" },
          ],
          hint: 'Two things at once: a normalised key for deciding sameness, and the original text for the output. Keep a set of keys you have already seen.',
          correct: 'def unique_names(names):\n    seen = set()\n    out = []\n    for n in names:\n        key = n.strip().lower()\n        if key not in seen:\n            seen.add(key)\n            out.append(n)\n    return out\n',
          wrong: 'def unique_names(names):\n    return sorted(set(n.strip().lower() for n in names))\n',
        },
        questions: [
          { id: 'd2-rd-1', prompt: 'What does set() cost you when you use it to remove duplicates?',
            choices: ['Nothing', 'The order of the items', 'The values themselves', 'Speed'],
            answer: 1,
            explain: 'A set has no order, so the output comes back in an arrangement you did not choose. When order carries meaning, that is a loss.' },
          { id: 'd2-rd-2', prompt: 'Why normalise before comparing names?',
            choices: ['It is faster', '"Ada" and " ada " are one person and two strings', 'Python requires it', 'To sort them'],
            answer: 1,
            explain: 'Case and stray spaces are the two most common ways the same value arrives looking different.' },
          { id: 'd2-rd-blank', kind: 'blank',
            prompt: 'To decide sameness you compare a normalised ___, while keeping the original text for the output.',
            blanks: [{ accept: ['key', 'version', 'form', 'value'] }],
            explain: 'Two representations, one for deciding and one for showing. Collapsing them is what makes cleaned data unreadable.' },
        ],
      },

      {
        slug: 'filtering-rows',
        title: 'Filtering Rows',
        summary: 'Selecting the rows you meant, and noticing how many you dropped.',
        sections: [
          ['A filter is a comprehension with a test',
            'Say the condition once, in one place. Scattering the same test through the code is how two parts of an analysis end up disagreeing about who counts.',
            'rows = [{"score": 92}, {"score": 58}, {"score": 88}]\npassing = [r for r in rows if r["score"] >= 60]\nprint(len(passing))'],
          ['Count what you removed',
            'A filter that quietly drops most of the data changes the answer more than any calculation after it. Printing the before and after is one line and catches it.',
            'rows = [{"score": 92}, {"score": 58}]\nkept = [r for r in rows if r["score"] >= 60]\nprint(len(rows), "->", len(kept))'],
        ],
        exercise: {
          prompt: 'Write <code>passing(rows, mark)</code> that returns the rows whose <code>score</code> is at least <code>mark</code>.',
          starter: 'def passing(rows, mark):\n    # The rows scoring mark or above.\n    return []\n\nprint(len(passing([{"score": 92}, {"score": 58}], 60)))\n',
          call: 'len(passing([{"score": 92}, {"score": 58}, {"score": 88}], 60))',
          expectValue: '2',
          hidden: [
            { name: 'a score exactly on the mark passes',
              call: 'len(passing([{"score": 60}], 60))', expect: '1' },
            { name: 'nobody passes an empty table',
              call: 'len(passing([], 50))', expect: '0' },
            { name: 'the rows themselves come back',
              call: 'passing([{"score": 92}], 60)', expect: "[{'score': 92}]" },
            { name: 'the boundary is included',
              kind: 'ast', requires: { compares: ['GtE'] },
              describe: 'a >= comparison, so the mark itself counts' },
          ],
          hint: 'At least the mark means the mark passes. A plain greater-than silently fails everyone sitting exactly on the boundary, which is where marks cluster.',
          correct: 'def passing(rows, mark):\n    return [row for row in rows if row["score"] >= mark]\n',
          wrong: 'def passing(rows, mark):\n    return [row for row in rows if row["score"] > mark]\n',
        },
        questions: [
          { id: 'd2-fr-1', prompt: 'Why write a filter condition in one place rather than repeating it?',
            choices: ['It is faster', 'Two copies can disagree after an edit, and both look right', 'Python requires it', 'It uses less memory'],
            answer: 1,
            explain: 'The second copy is the one nobody updates. Two parts of the same analysis then quietly count different people.' },
          { id: 'd2-fr-2', prompt: 'What should you check immediately after filtering?',
            choices: ['The column types', 'How many rows you have left', 'The file size', 'Nothing'],
            answer: 1,
            explain: 'A filter that keeps three rows out of nine hundred is a finding in itself, and usually a mistake in the condition.' },
          { id: 'd2-fr-3', prompt: 'A pass mark of 60 with a > test does what?',
            choices: ['Nothing different', 'Fails everyone who scored exactly 60', 'Fails everyone below 61', 'Raises an error'],
            answer: 1,
            explain: 'Both wordings describe the same off-by-one, and marks bunch at the boundary, so it usually affects more rows than you would guess.' },
        ],
      },

      {
        slug: 'sorting-and-ranking',
        title: 'Sorting and Ranking',
        summary: 'Order by the value you mean, and remember that sorted returns a new list.',
        sections: [
          ['Sorting by a field',
            'key says which part of each row to order by. reverse turns it around. sorted gives you a new list; .sort() changes the one you have and returns None.',
            'rows = [{"name": "Ada", "score": 92}, {"name": "Alan", "score": 79}]\nbest = sorted(rows, key=lambda r: r["score"], reverse=True)\nprint([r["name"] for r in best])'],
          ['The None that catches everyone',
            'names = names.sort() throws your list away and keeps None. It is the single most common list mistake, and nothing raises until you use the result.',
            'names = ["Grace", "Ada"]\nprint(names.sort())\nprint(names)\nprint(sorted(["Grace", "Ada"]))'],
        ],
        exercise: {
          prompt: 'Write <code>top_names(rows, n)</code> that returns the names of the <code>n</code> highest scorers, highest first.',
          starter: 'def top_names(rows, n):\n    # The n highest scorers, best first.\n    return []\n\nrows = [{"name": "Ada", "score": 92}, {"name": "Alan", "score": 79}]\nprint(top_names(rows, 1))\n',
          call: 'top_names([{"name": "Ada", "score": 92}, {"name": "Alan", "score": 79}, {"name": "Grace", "score": 88}], 2)',
          expectValue: "['Ada', 'Grace']",
          hidden: [
            { name: 'asking for none gives none',
              call: 'top_names([{"name": "Ada", "score": 92}], 0)', expect: '[]' },
            { name: 'asking for more than there is gives everyone',
              call: 'top_names([{"name": "Ada", "score": 92}], 5)', expect: "['Ada']" },
            { name: 'an empty table gives an empty list',
              call: 'top_names([], 3)', expect: '[]' },
            { name: 'the rows are sorted rather than assumed',
              kind: 'ast', requires: { calls: ['sorted'] },
              describe: 'sorted() ordering the rows' },
          ],
          hint: 'sorted with a key and reverse, then take a slice. A slice longer than the list is not an error, which is why asking for five out of one works.',
          correct: 'def top_names(rows, n):\n    best = sorted(rows, key=lambda r: r["score"], reverse=True)\n    return [r["name"] for r in best[:n]]\n',
          wrong: 'def top_names(rows, n):\n    best = sorted(rows, key=lambda r: r["score"])\n    return [r["name"] for r in best[:n]]\n',
        },
        questions: [
          { id: 'd2-sr-1', prompt: 'What does names.sort() return?',
            choices: ['The sorted list', 'None', 'A copy', 'The original order'],
            answer: 1,
            explain: 'It sorts in place and returns None. Assigning that result over your variable replaces the list with nothing, and nothing raises until later.' },
          { id: 'd2-sr-2', prompt: 'What is the key argument to sorted for?',
            choices: ['The column names', 'Deciding which part of each item to order by', 'Reversing the order', 'Removing duplicates'],
            answer: 1,
            explain: 'It is a function applied to each item, and the result is what gets compared. That is how you sort dictionaries by one of their values.' },
          { id: 'd2-sr-3', prompt: 'What does a slice of [:5] do to a list of one item?',
            choices: ['Raises IndexError', 'Returns the one item', 'Returns five copies', 'Returns an empty list'],
            answer: 1,
            explain: 'Slices clamp rather than raise, which is why taking the top ten of a short list needs no special case.' },
        ],
      },
    ],
  },
};
