# Pinned so a clean checkout reproduces REPORT.md. Python 3.9+.
# Deliberately separate from scripts/requirements.txt, which stays
# dependency-free for the site's own maintenance scripts.
numpy==2.0.2
pandas==2.2.3
scikit-learn==1.6.1
scipy==1.13.1
matplotlib==3.9.4
